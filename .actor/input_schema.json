{
    "title": "Coastal Water Level Tracker Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "state": {
            "title": "State",
            "type": "string",
            "editor": "textfield",
            "description": "Two-letter US state code to survey all tide stations in, e.g. \"CA\", \"NY\", \"FL\". Ignored if \"stationId\" is set."
        },
        "stationId": {
            "title": "Station ID",
            "type": "string",
            "editor": "textfield",
            "description": "A specific NOAA CO-OPS station ID to check instead of a whole state, e.g. \"8518750\" (The Battery, NY). Takes priority over \"state\" if both are set. Find IDs at tidesandcurrents.noaa.gov."
        },
        "product": {
            "title": "Reading type",
            "type": "string",
            "editor": "select",
            "description": "Which measurement to fetch.",
            "enum": ["waterLevel", "waterTemperature", "airTemperature", "airPressure"],
            "enumTitles": ["Water level (feet, relative to station datum)", "Water temperature", "Air temperature", "Barometric pressure"],
            "default": "waterLevel"
        },
        "maxResults": {
            "title": "Max results",
            "type": "integer",
            "editor": "number",
            "description": "Maximum number of stations to return when surveying a state. Stations with no current reading for the selected type are skipped.",
            "default": 25,
            "minimum": 1,
            "maximum": 100
        }
    },
    "required": []
}
